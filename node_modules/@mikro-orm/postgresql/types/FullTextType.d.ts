import { Type } from '@mikro-orm/core';
export declare class FullTextType extends Type<string, string> {
    compareAsType(): string;
    getColumnType(): string;
    convertToDatabaseValueSQL(key: string): string;
}
