import { MikroORM, type Options, type Configuration, type IDatabaseDriver } from '@mikro-orm/core';
import { PostgreSqlDriver } from './PostgreSqlDriver';
/**
 * @inheritDoc
 */
export declare class PostgreSqlMikroORM extends MikroORM<PostgreSqlDriver> {
    private static DRIVER;
    /**
     * @inheritDoc
     */
    static init<D extends IDatabaseDriver = PostgreSqlDriver>(options?: Options<D> | Configuration<D>, connect?: boolean): Promise<MikroORM<D>>;
}
export type PostgreSqlOptions = Options<PostgreSqlDriver>;
export declare function definePostgreSqlConfig(options: PostgreSqlOptions): Options<PostgreSqlDriver>;
