import { Express } from "express";
import { ConfigModule } from "../types/global";
declare const _default: ({ app, configModule, }: {
    app: Express;
    configModule: ConfigModule;
}) => Promise<void>;
export default _default;
